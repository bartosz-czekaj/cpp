#include <vector>
#include <iterator> 

using namespace std;

int solution(vector<int> &A) 
{
  int iEquiLeadres = 0;
  int iIdx = 0;
  
  std::vector<int>::iterator it;
  
    while(iIdx < A.size())
    {
      ++iIdx;
    }
  
  return iEquiLeadres;
}


int main()
{
  return 0;
}